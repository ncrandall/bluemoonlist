<%= render 'requests/request_form' unless @open_request %>
<div class="row">
	<span class="offset2 span8 text-right">
		<% if @filter %>
			<%= link_to 'all', feed_path %> | mine
		<% else %>
		  all | <%= link_to 'mine', "#{feed_path}?filter=self" %>
		<% end %>
	</span>
</div>
<%= render 'requests/requests' %>