<% if !@requests.any? %>
	<div class="row">
		<span class="offset2 span8 text-center">
			<p>Looks like there are no requests yet</p>
		</span>
	</div>
<% end %>
<%= render 'shared/row_objects', objects: @requests %>