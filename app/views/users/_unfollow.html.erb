<%= form_for(current_user.relationships.where(neighbor_id: @user.id).first, html: { method: :delete }, remote: true) do |f| %>
<%= f.hidden_field :neighbor_id %>
<%= f.submit "unfollow", class: "btn" %>
<% end %>